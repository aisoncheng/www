(function(){
	


	layui.useMine = function(m,ndir,c){
		if(ndir){
			var op = {};
			op[m] = ndir;
			layui.extend(op);
		}
		layui.use(m,c);
	}
	
	layui.useMine = function(m,ndir,c){
		if(ndir){
			var op = {};
			op[m] = ndir;
			layui.extend(op);
		}
		layui.use(m,c);
	}
	
	layui.extend({
	
	});
	
})();